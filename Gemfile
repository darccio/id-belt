# As good practice, I register used gems grouping them around which utility depends on them, with exact versions :)
source "http://rubygems.org"
group :json_loader do
  gem "sequel", "3.30.0"
  gem "yajl-ruby", "1.1.0"
end
group :emleaks do
  gem "eventmachine", "0.12.10"
end
